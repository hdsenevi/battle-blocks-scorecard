# Maestro E2E Testing Configuration
# For Expo managed workflow

# App configuration
# Bundle ID: au.com.sodamade.battleblocksscorecard (iOS bundleIdentifier / Android package)
# Set per-flow in .maestro/flows/*.yaml via appId. For EAS Build, bundle ID comes from app.json.

# Platform support
platforms:
  - ios
  - android

# Expo-specific configuration
# Maestro works with Expo development builds (not Expo Go)
# Ensure you have built a development build before running tests:
# - iOS: npx expo run:ios
# - Android: npx expo run:android
